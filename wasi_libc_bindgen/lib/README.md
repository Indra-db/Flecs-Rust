# Pre-compiled Libraries

This directory contains pre-compiled static libraries for distribution:

- `musl_top_half_libc.a` - WASM32 compiled libc functions (memcpy, malloc, etc.)

These libraries are generated by the build script when the `build-libc` feature is enabled.
